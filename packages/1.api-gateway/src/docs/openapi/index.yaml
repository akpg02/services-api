openapi: 3.0.0
info:
  title: eShop Gateway API
  version: 1.0.0

servers:
  - url: http://localhost:8000

paths:
  $ref: './bundled.yaml#/paths'

components:
  $ref: './bundled.yaml#/components'

security:
  - BearerAuth: []
