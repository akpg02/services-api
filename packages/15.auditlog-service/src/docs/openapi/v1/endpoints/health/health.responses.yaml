HealthSuccess:
  description: Service is healthy
  content:
    application/json:
      schema:
        $ref: './health.schema.yaml#/HealthResponse'

HealthError: # ← definition for the 400 response
  description: Bad request
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: Bad request
