patch:
  tags: [User]
  summary: Change email for the authenticated user
  operationId: change-email
  security: [{ BearerAuth: [] }]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../user.schema.yaml#/ChangeEmailRequest'
  responses:
    '200':
      $ref: '../users.responses.yaml#/UpdateSuccess'
    '400':
      $ref: '../users.responses.yaml#/ValidationError'
    '401':
      $ref: '../users.responses.yaml#/Unauthorized'
    '500':
      $ref: '../users.responses.yaml#/ServerError'
